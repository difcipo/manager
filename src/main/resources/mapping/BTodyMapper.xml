<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cai.mapper.TodyMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <select id="getTody" resultType="BTody" parameterType="Integer">
        select * from b_tody WHERE id = #{id}
    </select>



</mapper>